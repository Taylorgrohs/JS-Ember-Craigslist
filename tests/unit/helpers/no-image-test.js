import { noImage } from 'craigs-list/helpers/no-image';
import { module, test } from 'qunit';

module('Unit | Helper | no image');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = noImage([42]);
  assert.ok(result);
});
